---
apiVersion: v1
kind: Service
metadata:
  name: cache${BRANCH_HASH}
  labels:
    app: cache${BRANCH_HASH}
    branch: wif-bo${BRANCH_HASH}
spec:
  type: NodePort
  ports:
  - port: ${PORT}
  selector:
    app: cache${BRANCH_HASH}
