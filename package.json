{
  "private": true,
  "scripts": {
    "build": "lerna run build",
    "lint": "lerna run lint --parallel",
    "start": "lerna run start --parallel",
    "test": "lerna run test --parallel",
    "coverage": "lerna run test --parallel -- --coverage"
  },
  "devDependencies": {
    "lerna": "^3.13.4"
  },
  "workspaces": [
    "packages/*"
  ],
  "name": "work-in-france-backoffice"
}
